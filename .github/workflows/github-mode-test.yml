name: GitHub Mode Test

on:
  pull_request:
    paths:
      - ".GITHUB-MODE/**"
      - ".github/workflows/github-mode-*.yml"
  push:
    branches: [main]
    paths:
      - ".GITHUB-MODE/**"
      - ".github/workflows/github-mode-*.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608
      - uses: ./.github/actions/setup-node-env
      - name: Enforce GitHub Mode activation
        run: node --import tsx .GITHUB-MODE/scripts/check-github-mode-active.ts
      - name: Run GitHub Mode tests
        run: pnpm test -- .GITHUB-MODE/test/
      - name: Emit test evidence
        run: |
          mkdir -p "$RUNNER_TEMP/github-mode-test"
          cat > "$RUNNER_TEMP/github-mode-test/evidence.json" <<EOF
          {
            "workflow": "github-mode-test",
            "result": "pass",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "run_id": "${{ github.run_id }}",
            "sha": "${{ github.sha }}"
          }
          EOF
      - name: Upload test evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: github-mode-test-evidence
          path: ${{ runner.temp }}/github-mode-test/evidence.json
          retention-days: 90
