name: GitHub Mode Cost

on:
  pull_request:
    paths:
      - ".GITHUB-MODE/**"
      - ".github/workflows/github-mode-*.yml"
  push:
    branches: [main]
    paths:
      - ".GITHUB-MODE/**"
      - ".github/workflows/github-mode-*.yml"

permissions:
  contents: read

jobs:
  cost-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608
      - uses: ./.github/actions/setup-node-env
      - name: Enforce GitHub Mode activation
        run: node --import tsx .GITHUB-MODE/scripts/check-github-mode-active.ts
      - name: Validate cost thresholds contract
        run: node --import tsx .GITHUB-MODE/scripts/check-cost-thresholds.ts
      - name: Publish cost summary
        if: always()
        run: |
          {
            echo "## Cost check results"
            echo ""
            echo "Cost threshold validation completed for commit \`${{ github.sha }}\`."
            echo ""
            echo "- Run ID: \`${{ github.run_id }}\`"
            echo "- Trigger: \`${{ github.event_name }}\`"
            echo "- Thresholds: validated"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Emit cost evidence
        run: |
          mkdir -p "$RUNNER_TEMP/github-mode-cost"
          cat > "$RUNNER_TEMP/github-mode-cost/evidence.json" <<EOF
          {
            "workflow": "github-mode-cost",
            "result": "pass",
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "run_id": "${{ github.run_id }}",
            "sha": "${{ github.sha }}"
          }
          EOF
      - name: Upload cost evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: github-mode-cost-evidence
          path: ${{ runner.temp }}/github-mode-cost/evidence.json
          retention-days: 90
